# QK Tool Hub PDF 전용 도구 기획서

작성일: 2026-09-16
상태: 첫 출시·홈 정리·정책 안내까지 운영 배포 완료. 1~11절은 기획 당시의 목표와 출시 기준이고, 12~22절은 실제 구현 상태와 남은 작업을 기록한다.
기준 저장소: [ionic2001/qktoolhub](https://github.com/ionic2001/qktoolhub), 코드 커밋 `21f4b77`(원격 반영 후 운영 기준)
운영 사이트: https://www.qktoolhub.com/

## 1. 목표와 제품 경계

현재 `/pdf-converter`는 이미지→PDF 생성과 PDF 페이지→PNG 저장을 한 화면에 제공한다. 신규 제품 구조는 이미지 파일로 PDF를 만드는 도구와 **이미 존재하는 PDF 파일을 관리하는 도구**를 분리한다. PDF 전용 도구는 페이지 병합·분리·추출·정리를 주된 가치로 삼고, 이후 내용 추가와 양식 처리로 확장한다.

- **이미지→PDF:** JPG·PNG·WebP로 PDF 생성. 기존 이미지 준비·A4·방향·여백·순서·다운로드 기능을 유지한다.
- **PDF→PNG:** 기존 PDF 페이지 이미지 저장을 PDF 전용 도구의 출력 기능으로 이동한다.
- **PDF 전용 도구:** 이미 만들어진 PDF의 페이지를 병합·분리·정리하고 결과 PDF를 다운로드한다.

기존 설명이 가리키는 기능이 실제로 동작하도록 페이지와 메타정보를 함께 바꾼다. 새 기능이 출시되기 전에는 해당 기능을 홈 카드, 메타, sitemap이나 구조화 데이터에서 제공한다고 표현하지 않는다.

## 2. 대상 사용자와 핵심 흐름

- 여러 계약서·신청서·보고서를 한 파일로 모으는 사용자 → PDF 선택 → 파일·페이지 순서 확인 → 병합 → 결과 검토·다운로드.
- 긴 PDF에서 필요한 페이지만 전달하는 사용자 → PDF 선택 → 범위·개별 페이지 선택 → 새 PDF 생성 → 다운로드.
- 스캔 또는 제출용 문서의 페이지를 정리하는 사용자 → PDF 선택 → 회전·순서 변경·삭제 → 저장.
- PDF 내용을 이미지로 쓰는 사용자 → PDF 선택 → 페이지·해상도 선택 → PNG 저장.

모든 작업은 현재 도구와 일관되게 브라우저 안에서 처리한다. 사용자 파일의 텍스트·파일명·미리보기 이미지를 방문 분석 이벤트에 넣지 않는다. 파일은 페이지를 닫으면 다시 가져올 수 없으므로 결과 다운로드와 작업 종료 전 안내를 명확히 한다.

## 3. 정보 구조와 URL

| 역할 | 제안 URL | 첫 출시 |
|---|---|---|
| 이미지→PDF | `/image-to-pdf` | 예 |
| PDF 도구 안내·선택 | `/pdf-tools` | 예 |
| PDF 병합 | `/pdf-merge` | 예 |
| PDF 분리·추출 | `/pdf-split` | 예 |
| 페이지 정리 | `/pdf-organize` | 예 |
| PDF 페이지→PNG | `/pdf-to-png` | 예 |
| 내용 추가·워터마크 | `/pdf-edit` | 후속, 기능 완료 후 공개 |

각 작업 페이지에는 실제 조작 화면, 고유 제목·설명·사용 방법·제한 사항·관련 도구 링크가 있어야 한다. `/pdf-tools`는 네 작업의 링크와 비교 설명을 제공하는 안내 페이지이며 다른 페이지의 설명을 단순 복제하지 않는다. PDF 편집이라는 큰 검색어를 먼저 선점하기 위해 제한된 기능을 `/pdf-edit`로 노출하지 않는다.

**기존 `/pdf-converter` 이전:** 첫 출시에서는 기존 URL과 도구를 유지하면서 새 페이지로 연결하고, 고유한 이전 안내를 제공한다. 이 단계에서 새 6개 경로를 더하면 12개 기존 경로 → 18개, 5개 언어 기준 60개 → 90개 canonical URL이 된다. 기존 페이지와 새 페이지에 똑같은 본문·도구를 중복 배치하지 않는다. 이후 Search Console의 `/pdf-converter` 실제 검색어·유입 비중을 보고 이동 결정을 내린다: 이미지→PDF 유입이 주라면 `/image-to-pdf`로, PDF 이미지 유입이 주라면 `/pdf-to-png`로, 두 의도가 모두 뚜렷하면 고유 안내 페이지로 유지한다. 영구 이전 시 언어 쿼리와 사용자의 링크를 보존하고 기존 URL을 308/301로 새 URL에 연결한다. 리디렉션 후에는 기존 URL을 sitemap·hreflang·canonical 집합에서 제거한다. URL 이전은 리디렉션과 canonical을 일치시키는 것이 핵심이다. [Google의 canonical 및 리디렉션 안내](https://developers.google.com/search/docs/crawling-indexing/consolidate-duplicate-urls).

한국어는 쿼리 없는 URL, 영어·일본어·간체 중국어·스페인어는 기존의 `?lang=en|ja|zh|es` 방식을 유지한다. `?lang=ko`는 기본 한국어 URL을 canonical로 쓰는 별칭이다. 새로운 URL 패턴을 병행 도입하지 않는다.

## 4. 첫 출시 기능 명세

| 기능 | 상세 행동 | 처리 기준과 오류 |
|---|---|---|
| 병합 | PDF 여러 개 선택; 파일과 페이지 순서 표시; 순서 변경; 모든 페이지를 하나의 PDF로 저장 | 파일 선택 순서가 결과 순서로 반영. 일부 파일 실패 시 완성된 것처럼 다운로드하지 않고 실패 파일을 표시 |
| 분리 | 1개 PDF에서 `1-3, 5, 8-10` 같은 범위 또는 개별 페이지 선택; 선택한 페이지를 새 PDF로 저장 | 중복·역순·범위 밖·빈 선택을 명확히 처리. 원본 페이지 순서 유지가 기본값 |
| 추출 | 분리 화면에서 선택한 페이지를 **한 파일**로 저장 | 페이지별 개별 파일·ZIP 저장은 후속 검토 |
| 정리 | 페이지 미리보기; 버튼으로 순서 변경; 90도 회전; 선택 삭제; 빈 페이지 추가; 결과 저장 | 마지막 페이지 삭제를 막아 빈 PDF 생성 방지. 드래그 이동은 후속 검토 |
| PDF→PNG | 기존 페이지 선택·72/144/216 DPI·미리보기·개별 PNG 다운로드 유지 | 현재 50MB·300페이지 제한, 암호화 PDF 미지원, 흰 배경·최대 6MP 제한을 새 안내에도 반영 |
| 진행 상태 | 파일 로드, 페이지 파싱·미리보기, 결과 생성, 다운로드 준비를 분리 표시 | 작업 중 이중 실행 방지. 취소 가능 단계와 취소 불가 단계를 구별 |

기존 코드에는 `pdf-lib`과 PDF.js가 이미 있다. `pdf-lib`의 `copyPages`, `removePage`, `insertPage`, `save`, 페이지 회전 API로 위 페이지 작업을 설계할 수 있고 PDF.js로 미리보기를 렌더링할 수 있다. [pdf-lib PDFDocument API](https://pdf-lib.js.org/docs/api/classes/pdfdocument), [PDFPage API](https://pdf-lib.js.org/docs/api/classes/pdfpage), [PDF.js 브라우저 예제](https://mozilla.github.io/pdf.js/examples/). 미리보기는 빠른 탐색용으로 저해상도·필요 페이지부터 생성하고, 출력은 원본 PDF 페이지를 복사하는 경로를 우선해 불필요한 이미지 변환을 피한다.

**초기 운영 한도:** 병합 최대 10파일, 파일당 50MB, 총 입력 100MB, 결과 최대 300페이지; 분리·정리는 파일당 50MB·300페이지. 이미지→PDF는 최대 20장, 장당 20MB·총 100MB이며 큰 이미지는 긴 변 2,400px로 축소한다. PDF→PNG는 50MB·300페이지·최대 600만 픽셀을 적용한다. 모바일 메모리와 처리 시간을 측정해 필요하면 조정한다.

## 5. 후속 기능과 명칭 제한

| 기능 | 우선순위 | 개발 시 조건 |
|---|---|---|
| 페이지 번호·워터마크·텍스트·이미지 덧붙이기 | 2 | `PDF 수정`보다 `내용 추가`로 정확하게 안내. 한글·일본어·중국어 글꼴 임베딩과 좌표·회전 검증 필요 |
| PDF 양식 채우기 | 2 | 지원되는 대화형 양식 필드만 처리. 플래튼 여부를 사용자가 선택할 수 있도록 검토. [pdf-lib 양식 API](https://pdf-lib.js.org/docs/api/classes/pdfform) |
| PDF 문서 속성 변경 | 2 | 제목·작성자·주제 등 메타데이터 값 표시·수정; 민감한 작성자 정보 처리 안내 |
| PDF 텍스트 추출 | 3 | 텍스트 레이어가 있는 문서만. 추출 순서·표·다단 편집 오류 검증 |
| OCR | 3 | 스캔 PDF는 별도 인식 기술과 언어 모델, 성능·데이터 처리 정책 결정 후 제공 |
| 파일 크기 줄이기 | 3 | 단순 저장으로 압축을 보장하지 않음. 이미지 재압축·페이지 래스터화는 화질·검색 가능 텍스트·접근성을 해칠 수 있으므로 별도 품질 기준 필요 |
| 암호화 PDF 입력·비밀번호 보호 | 3 | 현재 지원하지 않음. `ignoreEncryption`을 암호 해제나 보안 우회로 사용하지 않음. 호환성·권한 검증 필요 |
| 서명 그림 추가 | 3 | 그림을 문서에 넣는 기능이며 암호학적 디지털 서명으로 표현하지 않음. pdf-lib는 디지털 서명 생성·검증 전용 API를 제공하지 않는다. [공식 서명 API 안내](https://pdf-lib.js.org/docs/api/classes/pdfsignature) |
| 진짜 가리기(redaction), 기존 문장 직접 교체 | 별도 연구 | 흰 사각형 덮기는 원본 데이터 제거가 아니므로 비공개 정보 삭제 기능으로 출시하지 않음 |

서명된 문서, 양식, 주석, 첨부·책갈피가 있는 문서를 병합·정리하면 결과의 구조나 서명 유효성이 달라질 수 있다. 이를 모두 보존한다고 약속하지 않고 샘플 문서별 보존 검사를 먼저 수행한다. 서명된 입력은 명확한 경고 또는 초기 출시 제외를 결정한다.

## 6. 화면과 사용성

- PDF 전용 홈에서 병합·분리·정리·PNG 기능을 **큰 카드와 짧은 비교 설명**으로 구분한다.
- 각 도구는 업로드 영역 → 미리보기·선택 → 결과 설정 → 저장 버튼의 일관된 순서를 사용한다.
- 파일 선택 뒤 원본 파일명·크기·페이지 수·암호화 여부 또는 읽기 실패를 표시한다. 결과 파일명은 바꿀 수 있고 기본값에는 작업 유형을 포함한다.
- 페이지 번호는 사용자에게 1부터 보여주고 내부 배열 0 기반 번호로 변환할 때 한 곳에서 검증한다.
- 드래그 앤 드롭에만 의존하지 않는다. 키보드로 파일 선택, 페이지 선택, 위·아래 이동, 삭제·회전이 가능하다.
- 모바일에서는 썸네일 그리드와 조작 버튼을 축소·재배치하고 큰 PDF를 열 때 메모리 한도를 안내한다.
- 오류 문구는 `읽을 수 없음`, `암호화`, `손상`, `범위 오류`, `한도 초과`, `메모리 부족`, `결과 생성 실패`를 구분하고 원본 파일은 다시 선택하도록 안내한다.
- 자동 업로드·자동 저장은 사용자의 예상과 다르므로 추가하지 않는다. 분석 이벤트는 기능 종류, 성공·실패 범주, 처리 시간의 넓은 구간 정도만 수집한다. 개인 문서 정보는 수집하지 않는다.

## 7. SEO와 콘텐츠 설계

새 **6개 경로 각각** 5개 언어의 고유 H1·title·description·사용 안내를 제공한다. 기능 페이지에 실제 도구가 있고 그 아래에 사용 방법, 지원 파일, 주의점, 흔한 오류, 관련 PDF 도구를 배치한다. PDF 도구 홈은 각 기능의 차이와 선택 기준을 설명한다. 키워드를 나열하거나 존재하지 않는 기능을 메타에 적지 않는다. Google은 페이지별로 정확하고 고유한 설명을 권장하며 메타 설명에 고정 글자수 상한은 없다. [Google 메타 설명 지침](https://developers.google.com/search/docs/appearance/snippet).

| 페이지 | 검색 의도와 고유 본문 |
|---|---|
| `/image-to-pdf` | JPG·PNG·WebP 여러 장을 한 PDF로 만드는 절차; 해상도·여백·배경·다운로드 |
| `/pdf-tools` | PDF 작업 선택: 병합과 분리의 차이, 페이지 정리와 PNG의 목적 |
| `/pdf-merge` | 여러 PDF의 파일 순서와 페이지 순서, 출력 문서 확인, 서명·양식 주의점 |
| `/pdf-split` | 페이지 범위 입력 예시, 추출·분리 출력 차이, 오류 입력 해결 |
| `/pdf-organize` | 페이지 이동·회전·삭제·빈 페이지 추가의 사용 사례와 저장 안내 |
| `/pdf-to-png` | 페이지별 PNG와 DPI·용량·흰 배경·해상도 차이 |

홈 → PDF 도구 홈 → 각 도구, 이미지 도구 → 이미지→PDF, PDF 전용 페이지 서로 간에 실제 `<a href>` 링크를 제공한다. 페이지 이동 버튼만으로 크롤러 탐색을 구성하지 않는다. [Google 링크 지침](https://developers.google.com/search/docs/crawling-indexing/links-crawlable).

### 메타태그·URL 체크리스트

각 페이지·언어의 **최초 HTTP HTML**에 다음을 넣고 실행 후 메타가 다른 페이지 것으로 덮이지 않는지 확인한다.

- 고유 `<title>`, `meta[name=description]`, 정확한 `<html lang>`, H1 한 개와 읽을 수 있는 본문.
- `meta[name=robots]`: 출시한 작업 페이지는 `index, follow`; 오류·임시 결과·미리보기 페이지는 `noindex`. 작업 상태를 공유 가능한 쿼리 URL로 만들더라도 색인하지 않는다.
- `link[rel=canonical]`: www 도메인의 자기참조 공개 URL. 한국어 별칭 `?lang=ko`는 쿼리 없는 한국어 URL을 지정한다.
- `hreflang` 5개 + `x-default`를 각 언어의 HTML에서 상호 참조; sitemap에도 동일한 URL 집합. [Google 다국어 URL 안내](https://developers.google.com/search/docs/specialty/international/localized-versions).
- OG `type`, `site_name`, `title`, `description`, `url`, `image`, `image:alt`, `locale`; Twitter/X 카드·제목·설명·이미지. 공유 이미지가 실제 PNG로 응답하는지 확인.
- robots.txt가 새 경로를 차단하지 않는지, sitemap은 200·index 가능한 자기참조 canonical만 수록하는지 확인. 신뢰할 수 없는 빌드 일자를 `lastmod`로 넣지 않는다.
- 구조화 데이터: 현재의 `WebPage`, 실제 사용 가능한 페이지의 `WebApplication`, 사용자 경로와 맞는 `BreadcrumbList`; 무료 이용 가격은 실제로 무료일 때만 `Offer.price=0`. 같은 JSON-LD 내용이 정적 HTML과 실행 후 문서에 중복되지 않게 한다. Google의 앱 리치 결과 조건은 [공식 문서](https://developers.google.com/search/docs/appearance/structured-data/software-app)를 따르고, 표시를 보장하지 않는다.
- FAQ는 화면에 보이는 실제 질문과 답만 작성한다. FAQ 스키마를 검색 결과 표시를 늘리는 핵심 작업으로 보지 않는다. Google의 FAQ 리치 결과는 대부분 권위 있는 정부·건강 사이트에 제한된다. [공식 발표](https://developers.google.com/search/blog/2023/08/howto-faq-changes).

### 운영 제목·설명과 번역 계약

아래는 현재 도구 제목의 본문 부분이다. 페이지 `<title>`에는 공통으로 ` | QK Tool Hub`가 붙는다.

| 기능 | 한국어 제목 | 영어 제목 |
|---|---|---|
| 이미지→PDF | 이미지 PDF 변환기 · JPG·PNG를 PDF로 | Images to PDF Converter · JPG & PNG to PDF |
| PDF 홈 | 무료 PDF 도구 · 병합·분리·페이지 정리 | Free PDF Tools · Merge, Split & Organize PDFs |
| 병합 | PDF 병합 · 여러 파일을 하나로 | Merge PDF Files Online |
| 분리 | PDF 분리·페이지 추출 | Split PDF & Extract Pages |
| 정리 | PDF 페이지 순서 변경·회전·삭제 | Organize PDF Pages · Reorder, Rotate & Delete |
| PNG | PDF PNG 변환 · 페이지별 이미지 저장 | PDF to PNG · Export Individual Pages |

각 페이지 설명은 **입력 → 주요 조작 → 결과 → 실제 제한**을 1~2문장으로 쓴다. 예: 한국어 병합은 “여러 PDF를 원하는 순서로 합쳐 하나의 파일로 저장하세요. 페이지를 확인하고 브라우저에서 처리한 결과를 다운로드할 수 있습니다.” 영어는 “Merge multiple PDFs in the order you choose. Review the pages and download one combined file, processed in your browser.”

| 기능 | 일본어 용어 | 간체 중국어 용어 | 스페인어 용어 |
|---|---|---|---|
| 이미지→PDF | 画像をPDFに変換 | 图片转PDF | Imágenes a PDF |
| PDF 홈 | 無料PDFツール | 免费PDF工具 | Herramientas PDF gratis |
| 병합 | PDFを結合 | 合并PDF | Unir archivos PDF |
| 분리 | PDFを分割・ページを抽出 | 拆分PDF·提取页面 | Dividir PDF y extraer páginas |
| 정리 | PDFページを並べ替え・回転 | 整理PDF页面·排序和旋转 | Organizar páginas PDF |
| PNG | PDFをPNGに変換 | PDF转PNG | PDF a PNG |

표의 세 언어 용어는 기획 당시 기준이며 실제 운영 제목은 `src/i18n/pdfWork.ts`를 확인한다. 각 언어의 title에는 브랜드를 붙이고, description은 실제 입력·작업·다운로드와 한도를 반영해 별도로 작성했다. 일본어의 結合/分割, 중국어의 合并/拆分, 스페인어의 unir/dividir 용어를 버튼·가이드·메타에서 일관되게 관리한다. 검색 결과의 현지 표현은 실제 노출 데이터를 얻은 뒤 재검토한다.

일본어·간체 중국어·스페인어도 같은 기능 범위를 번역하되 검색어 표현은 자연스러운 현지 용어로 검수한다. `PDF 병합`, `PDF 분리`, `페이지 추출`, `회전`, `양식` 등 용어집을 만들고 UI·title·description·본문·파일명에 일관되게 적용한다. 번역이 준비되지 않은 언어 URL을 한국어·영어 본문으로 생성하지 않는다.

## 8. 개발 변경 지점

현재 저장소에서 수정할 곳을 다음 순서로 추적한다.

1. `src/pages/PdfConverter.tsx`: 이미지→PDF와 PDF→PNG 로직을 재사용 가능한 구성 요소·유틸리티로 분리. 기존 동작을 유지하면서 새 페이지에 연결.
2. `src/utils/pdfTools.ts`: 원본 페이지 복사 기반 병합·분리·정리 순수 작업 함수와 입력 검증, 결과 Blob 생성. 작업별 함수가 파일 읽기·UI 상태를 직접 섞지 않도록 설계.
3. `src/App.tsx`: 새 경로 추가, 로딩과 실제 404 경로 유지.
4. `src/pages/Home.tsx`, `src/seo/SeoSections.tsx`, Header/Footer: 카테고리·관련 도구·언어별 실제 링크 업데이트.
5. `src/seo/catalog.ts`, `src/seo/content.json`, PDF 번역 파일: 5개 언어의 페이지 이름·메타·설명·schema·용어 관리.
6. `scripts/build-seo.mjs`: 모든 새 경로의 초기 HTML 설명·가이드·내부 링크 생성. PDF 작업 UI는 JS로 실행돼도 검색용 본문과 링크는 초기 HTML에 존재해야 한다. [Google JS SEO 설명](https://developers.google.com/search/docs/crawling-indexing/javascript/javascript-seo-basics).
7. `vercel.json`: 새 언어별 재작성, 직접 `.html` 경로 리디렉션, 기존 URL 이전 규칙 및 실제 404 유지. 기존 외국어 홈 문제를 막기 위해 빌드 후 root index 제거 동작을 보존.
8. `scripts/test-seo.mjs`: 경로×언어 기대 URL을 코드 데이터에서 계산하게 하여 고정된 60개 숫자에 의존하지 않도록 갱신.
9. 개인정보처리방침·사용 안내: 실제 파일 처리와 분석 범주가 변하면 함께 갱신. 기능 설명과 법적 문서의 수집·저장 주장을 대조.

폰트 파일, PDF.js worker, cMaps, WASM, 표준 폰트 등 필요한 빌드 자산의 제공 여부와 배포 MIME 유형을 확인한다. 도구 전용 코드는 필요한 페이지에서 지연 로딩하고 홈 첫 화면에 무거운 PDF 라이브러리를 넣지 않는다.

## 9. 데이터·안전·품질 기준

- 입력은 사용자가 선택한 로컬 PDF만. 파일 확장자가 PDF여도 헤더·파싱 결과·페이지 수를 확인한다. 손상·암호화 문서와 비정상적으로 큰 페이지를 구분.
- 결과는 사용자 기기에서 만들고 명시적 다운로드로만 저장한다. 선택한 문서의 내용·파일명·페이지 썸네일을 서버·광고·분석에 전송하지 않는다.
- 여러 파일을 합친 출력에 원본 파일의 메타데이터·책갈피·주석·양식·서명이 모두 보존된다고 약속하지 않는다. 실제 샘플을 이용해 보존 범위를 확인하고 화면에 안내.
- 한국어·일본어·중국어 내용 추가를 출시할 때 지원 글꼴 포함, 글자 누락·줄바꿈·회전 좌표를 검사.
- 웹 접근성: label, 오류 메시지 연결, 키보드 조작, 초점 관리, 진행 상태 `role=status`, 버튼 비활성 이유, 색상 외 구분 수단.
- 메모리: 전체 페이지 썸네일을 한꺼번에 렌더링하지 않고 보이는 범위만 생성; 작업 종료·파일 교체 시 Blob URL과 PDF.js task를 해제. 모바일·저사양 환경에서 메모리 부족을 별도 테스트.
- 광고는 파일 선택·다운로드 조작을 가리거나 오작동시키지 않는다. PDF 작업 중 광고 스크립트가 민감한 문서 데이터를 접근하거나 분석 이벤트가 내용을 담지 않는지 확인.

## 10. 검증과 출시 게이트

| 검증 범주 | 통과 조건 |
|---|---|
| 병합 | 2~10개 파일의 출력 페이지 수·내용·순서가 입력 선택과 일치. 다른 크기·방향의 페이지 유지. 한 파일 손상 시 잘못된 결과 다운로드 금지 |
| 분리·추출 | 범위 `1-3,5,8-10`, 한 페이지, 첫·마지막 페이지, 중복·역순·범위 밖 처리. 출력 열어서 페이지 순서 확인 |
| 정리 | 위·아래 이동·회전·삭제·빈 페이지 추가가 미리보기와 저장 결과에서 일치. 취소·다시 실행 또는 변경 초기화 정책 명확 |
| 기존 기능 | 이미지→PDF와 PDF→PNG의 기존 입력 한도, 품질·다운로드·모바일 조작 회귀 통과 |
| 특수 입력 | 손상 PDF, 암호화 PDF, 스캔 PDF, 양식, 서명·주석, 큰 페이지, 유니코드 파일명, 같은 이름 파일 처리 |
| 브라우저 | 최근 Chrome/Safari/Edge, 데스크톱·모바일 대표 크기에서 업로드·미리보기·다운로드. 메모리·작업 시간 기록 후 한도 확정 |
| 보안·개인정보 | 파일 내용·이름의 네트워크 전송 없음; 민감 문서 내용이 분석·오류 로그에 없음; Blob URL·작업 데이터 정리 |
| SEO | 모든 공개 경로×5언어의 200, 자기참조 canonical, 단일 H1, 고유 title/description, 6개 언어 연결, 유효 JSON-LD, 실제 `<a href>` 내부 링크, sitemap 일치. 존재하지 않는 기능 문구 없음 |
| 운영 | Vercel 미리보기 확인 → 배포 후 새 URL·기존 URL·robots·sitemap·공유 이미지·404 실응답 확인. Search Console에서 대표 URL 검사와 기존 URL의 이전 상태 확인 |

배포 직후 순위 상승을 출시 조건으로 두지 않는다. Search Console에서 색인·검색어·클릭률이 쌓인 후 제목·설명을 개선한다. 결과의 내용 보존과 다운로드 성공이 먼저다.

## 11. 구현 순서와 결정해야 할 항목

1. **현행 기능 분리:** 이미지→PDF와 PDF→PNG 로직 추출, 기존 `/pdf-converter` 동작 회귀 검사.
2. **PDF 작업 엔진:** 병합·분리·추출·페이지 정리의 입력 검증과 원본 페이지 복사 기반 결과 생성.
3. **UI와 다국어:** 5개 언어 도구 화면, 키보드·모바일 조작, 오류·제한 안내.
4. **정적 SEO와 URL:** 새 6개 페이지 초기 HTML, 메타·schema·내부 링크·sitemap·Vercel 규칙.
5. **배포 전 결정:** 실제 GSC 데이터에 따라 `/pdf-converter`의 유지 또는 이전 대상; 병합 한도와 개별 추출 ZIP 여부; 서명·양식 있는 파일의 지원 범위.
6. **미리보기·운영 검증:** 기능·성능·60→90개 URL 감사, Search Console 대표 URL 검사.
7. **후속 기능:** 내용 추가·워터마크·양식 채우기는 별도 기능 검증과 번역·SEO 페이지 준비 후 출시.

1~11절의 후속 기능과 검사 기준은 기획안이다. 12절 이후의 구현 기록을 현재 제공 기능의 기준으로 사용한다. 기존 URL 이전과 입력 한도 변경은 실제 성능 및 검색 데이터 확인 후 결정한다.

## 12. 첫 구현 범위 기록 (2026-09-16)

첫 구현은 이미지→PDF, PDF 도구 홈, 병합, 범위 선택 후 **한 PDF로 페이지 추출**, 페이지 순서 변경·90도 회전·삭제·빈 페이지 추가, PDF→PNG를 5개 언어의 고유 경로로 제공한다. 기존 `/pdf-converter`는 바로 이전하지 않고 계속 제공한다. PDF 결과는 브라우저에서 생성하며, 페이지 작업은 원본 PDF 페이지를 복사한다. 페이지별 개별 파일·ZIP 저장, 드래그로 페이지 이동, 내용 추가·양식·OCR·압축·암호·서명 편집은 후속 검토 범위다.

현재 적용한 병합 한도는 최대 10파일, 파일당 50MB, 총 100MB, 결과 최대 300페이지다. 분리·정리는 파일당 50MB·300페이지다. 이는 초기 브라우저 한도이며 실제 모바일 성능을 관찰해 조정한다. 초기 HTML·메타·canonical·hreflang·JSON-LD·sitemap은 기존 60개에서 90개 언어별 URL로 확장했다. `scripts/test-pdf.mjs`는 실제 PDF를 생성·저장·재열어 병합 순서, 범위 검증, 페이지 추출, 회전과 빈 페이지 삽입을 검사한다.

## 13. 메인페이지 PDF 메뉴 정리 (2026-09-16)

메인 카드에는 이미지로 PDF 한 파일을 만드는 `/image-to-pdf`와 기존 PDF에서 작업을 선택하는 `/pdf-tools`만 표시한다. 병합·페이지 추출·페이지 정리·선택 페이지 PNG 저장은 네 개의 짧은 바로가기에서 각각 `/pdf-merge`, `/pdf-split`, `/pdf-organize`, `/pdf-to-png`로 연결한다. `/pdf-split`은 현재 선택 페이지를 **한 PDF 파일**로 저장하므로, 메인 메뉴에서 페이지별 여러 파일 분리라고 표현하지 않는다. `/pdf-to-png`도 선택한 페이지를 저장한다고 안내한다.

기존 `/pdf-converter` 카드는 메인에서 제거하지만 색인된 주소와 기능, 고유 메타, sitemap URL은 유지한다. 이 단계의 sitemap은 90개 언어별 canonical URL로 계속 제공한다. 홈페이지 5개 언어의 설명·카테고리·초기 HTML 링크를 메뉴와 함께 업데이트하고, `/pdf-converter`의 리디렉션 여부는 Search Console의 실제 유입 의도를 더 확인한 뒤 결정한다.

## 14. 메인 하단 설명과 서비스·정책 안내 (2026-09-16)

메인 하단에 5개 언어의 PDF 설명 문단을 추가했다. 이미지로 새 PDF를 만드는 기능과 기존 PDF의 병합·선택 페이지 추출·정리·PNG 저장을 구분하며, 선택 페이지 추출 결과는 **새 PDF 한 파일**, PNG 결과는 **선택 페이지 이미지**라고 명시한다. 암호 PDF, OCR, 원문 텍스트 수정은 현재 제공하지 않는다. 페이지 작업 뒤 서명·양식·책갈피가 유지되지 않을 수 있으므로 중요한 출력은 다운로드 후 확인하도록 안내한다. 추가 문단은 메뉴 카드나 바로가기를 중복하지 않는다.

서비스 소개에서 오래된 “8개 도구” 목록을 제거하고 현재 텍스트·이미지·계산·시간 및 PDF 도구 범위를 설명한다. 문의에는 사용 도구·브라우저·재현 단계·PDF 오류 문구를 적도록 안내하고, 원본 문서를 첨부하지 않아도 문의할 수 있다고 밝힌다. 이용약관은 현재 PDF 작업 범위와 출력의 보존 제한을, 개인정보처리방침은 PDF 내용·미리보기의 브라우저 처리와 파일명·본문·페이지 이미지의 분석 이벤트 제외 설계를 설명한다. 사용자가 문의 메일에 직접 문서를 첨부하면 이메일 서비스가 그 첨부를 처리할 수 있다는 점도 별도로 안내한다. 약관·방침의 개정 표기는 2026-09-16이다.

홈·소개·약관·방침의 한국어·영어·일본어·간체 중국어·스페인어 본문과 메타 설명을 함께 갱신했다. 소개·약관·방침의 관련 도구 링크는 PDF 홈, 이미지→PDF 및 대표 도구로 연결한다. 모든 안내는 첫 HTTP HTML에도 들어가며, 새 도구 경로를 추가하지 않아 sitemap은 18개 경로 × 5개 언어 = **90개 canonical URL**을 유지한다.

## 15. 배포 검증과 남은 결정

운영 코드 `aebdcad`에서 TypeScript·Vite·정적 SEO 빌드, 90개 URL의 메타·canonical·hreflang·JSON-LD·링크·sitemap 검사, PDF 병합·추출·정리 검사와 테마 검사가 통과했다. GitHub `main` 반영 후 Vercel 성공 상태를 확인했고, 공개 홈 5개 언어와 소개·약관·방침 대표 언어 응답에서 새 문구·canonical·sitemap 90개를 확인했다. 전체 모바일·대형/암호/서명/양식 PDF 표본 검수와 Search Console의 실제 색인·유입 데이터 분석까지 완료했다고 기록하지 않는다.

다음 결정은 (1) Search Console의 `/pdf-converter` 유입 의도에 따라 고유 페이지 유지 또는 언어 보존 리디렉션, (2) 모바일 성능과 실패 유형을 보고 입력 한도 조정, (3) 페이지별 개별 추출·ZIP, 드래그 정리, 워터마크·양식·OCR 등 후속 기능의 별도 검증 순서다. 운영에서 제공하지 않는 기능은 홈·메타·sitemap에 먼저 노출하지 않는다.

## 16. Low value content 대응용 원본 가이드 1차 출시

애드센스의 Low value content 사유에 대응하기 위해 Blogger를 분리하지 않고 QK Tool Hub 내부 `/guides`에 한국어 원본 콘텐츠를 추가했다. 첫 글 세 개는 일반적인 PDF 설명을 재작성한 글이 아니라 운영 코드·자동 검사·출력 표본에 근거한다.

- `/guides/pdf-merge-browser-test`: 너비 500·510·520pt와 90도 회전 표본을 병합한 뒤 출력 파일을 다시 열어 페이지 순서·크기·회전을 검증한다.
- `/guides/pdf-page-extraction-test`: 범위 입력의 허용·거부 사례, 역순 추출, 재배치·회전·빈 페이지 결과를 표로 공개한다.
- `/guides/image-formats-to-pdf`: JPG·PNG·WebP가 흰 배경 JPEG 품질 0.92로 정규화되고 긴 변 2,400px로 축소되는 실제 구현을 설명한다.

병합 입력·결과와 페이지 정리 결과 PDF 5개를 개인정보 없는 다운로드 표본으로 제공한다. 테스트 스크립트가 공개 표본을 다시 열어 글에 적힌 너비·회전값과 일치하는지 확인한다. 각 글에는 작성·검토 주체, 날짜, 검증 방법, 확인하지 않은 범위, 관련 도구 링크를 표시한다.

가이드는 한국어만 준비했으므로 `?lang=` 번역 URL과 hreflang을 만들지 않는다. `/guides`는 CollectionPage, 상세 글은 Article과 3단계 BreadcrumbList를 사용한다. 기존 90개 다국어 URL에 한국어 가이드 4개를 더해 sitemap은 94개 canonical URL이 된다. 외국어 본문은 실제 현지화 검수 후 개별 URL로 추가한다.

## 17. 원본 가이드 2차 확대와 도구 연결

PDF 콘텐츠 묶음을 세 글 더 추가해 상세 가이드를 6개로 확대했다. `/guides/pdf-browser-privacy`는 브라우저 메모리의 파일 처리와 호스팅·분석·광고 통신을 구분하고 사용자가 Network 탭으로 확인하는 절차를 설명한다. `/guides/pdf-to-png-dpi-test`는 운영 렌더 계산을 공용 함수로 분리해 A4의 72·144·216 DPI 크기와 A3의 600만 픽셀 제한 결과를 자동 검사한다. `/guides/choose-pdf-tool`은 병합·추출·정리·PNG·이미지→PDF를 입력과 결과 파일 기준으로 비교한다.

한국어 이미지→PDF, PDF 홈, 병합, 추출, 정리, PDF→PNG 및 기존 변환 페이지의 초기 HTML에서 관련 가이드로 직접 연결한다. SEO 검사에는 도구별 가이드 링크 존재 여부와 새 글의 고유 본문·Article schema·rewrite·sitemap을 포함한다. 다국어 90개 URL과 한국어 가이드 목록·상세 7개를 합쳐 sitemap은 97개 canonical URL이다.

## 18. PDF 밖의 원본 콘텐츠 확대

Low value content 대응이 PDF 한 주제에만 머물지 않도록 글자수·단위 변환·이미지 편집의 실제 계산을 검증한 한국어 가이드 세 개를 추가했다. 한글·영문·이모지는 운영 글자수 함수로 글자수와 UTF-8 바이트를 비교하며 EUC-KR이 실제 인코딩이 아닌 추정치임을 명시한다. MB와 MiB는 운영 단위 계수로 1,000,000바이트와 1,048,576바이트 차이를 검사한다. 이미지 크기는 비율 잠금과 64~4096px 제한·반올림을 화면과 공용으로 쓰는 함수로 분리해 자동 검사한다.

각 도구의 한국어 초기 HTML에서 해당 가이드로 연결하고 상세 글에는 Article schema, canonical, 검증 방법과 관련 도구 링크를 제공한다. 준비되지 않은 외국어 번역 URL이나 hreflang은 만들지 않는다. 다국어 90개 URL과 한국어 가이드 목록·상세 10개를 합쳐 sitemap은 100개 canonical URL이다.

## 19. 환율·세계시간·달력 원본 콘텐츠와 신뢰성 수정

환율 교차계산, 뉴욕 서머타임 시작 경계, 한국 영업일 포함 기준을 운영 함수로 검증한 한국어 가이드 세 개를 추가했다. 환율 글의 숫자는 현재 환율이 아니라 1 USD=1,350 KRW 고정 시험값임을 명시한다. 외부 환율 API가 실패하면 고정 참고값임을 5개 언어로 표시하고, 실제 과거 데이터가 없을 때 파형으로 추세를 생성하던 로직은 제거했다.

각 도구의 한국어 초기 HTML에서 관련 글로 연결한다. 가이드 상세는 Article schema와 자기참조 canonical을 사용하며 준비되지 않은 번역 hreflang은 생성하지 않는다. 다국어 90개 URL과 한국어 가이드 목록·상세 13개를 합쳐 sitemap은 103개 canonical URL이다.


## 20. 교차 도구 핵심 콘텐츠: 온라인 서류 제출 절차

단일 도구 설명을 늘리는 대신 실제 사용자가 완료하려는 작업을 기준으로 `/guides/online-document-submission-checklist`를 추가했다. 가상의 제출 조건을 먼저 기록하고 자기소개서 글자수·바이트 확인, 사진 픽셀·비율 조정, 스캔 이미지의 PDF 변환, 기존 PDF 병합·페이지 정리, 최종 개인정보·파일 검수까지 7단계로 연결한다. 예시 규격은 특정 기관의 기준이 아니며 제출 화면의 최신 조건을 우선하도록 명시한다.

글자수 세기, 이미지 편집, 이미지→PDF, PDF 홈, 병합, 페이지 정리 화면의 한국어 초기 HTML에서 이 가이드로 연결한다. 개인정보 없이 복사해 쓸 수 있는 UTF-8 체크리스트도 함께 제공하며 빌드 검사가 파일과 제목을 확인한다. 상세 글 13개와 가이드 목록, 다국어 90개 URL을 합쳐 sitemap은 **104개 canonical URL**이다. 이 형식은 이후 `스캔 문서 제출`, `해외 지원서 파일 준비`처럼 실제 과업과 자체 도구를 연결하되 중복 문장을 양산하지 않는 핵심 콘텐츠의 기준으로 사용한다.


## 21. 스캔 문서 제출용 PDF 핵심 가이드

`/guides/scanned-documents-to-pdf-workflow`는 종이 문서를 휴대전화로 촬영하거나 스캐너로 저장한 뒤 제출용 PDF 한 파일로 완성하는 과정을 다룬다. 제출 조건 확인, 빛 반사·그림자·잘림 검수, 이미지 방향·여백·비율 정리, 페이지 누락·순서 확인, 용지와 여백 설정, 다운로드 결과 확대 검수, PDF 페이지 재정리의 순서로 구성했다. OCR·공인 전자서명·진위 확인·법적 원본성을 제공하거나 보증하지 않는 범위도 명시한다.

이미지 편집, 이미지→PDF, PDF 페이지 정리와 PDF 도구 홈의 한국어 초기 HTML에서 가이드로 연결한다. 촬영 품질·페이지 구성·최종 가독성을 확인하는 UTF-8 체크리스트를 다운로드 자료로 제공한다. 상세 가이드 14개와 가이드 목록, 다국어 90개 URL을 합쳐 sitemap은 **105개 canonical URL**이다.


## 22. PDF 제출 오류 증상별 문제 해결 가이드

`/guides/pdf-submission-error-troubleshooting`은 PDF 제출 실패를 파일 선택, 도구 처리, 결과 검수, 제출 사이트 접수 단계로 나눈다. 파일 형식·암호·손상, 입력 한도와 제출처 용량 차이, 페이지 누락·순서·회전, 촬영본 가독성, 전자서명·양식·책갈피, 브라우저 메모리와 다운로드 문제를 증상별로 설명한다. 기존 PDF 압축·암호 해제·OCR·디지털 서명 생성/검증은 현재 제공하지 않는다는 점을 각 해결 경로와 함께 명시한다.

이미지→PDF, PDF 홈, 병합, 페이지 추출, 페이지 정리와 PDF→PNG의 한국어 초기 HTML에서 가이드로 연결한다. 사용자가 민감한 원본을 공유하지 않고 실패 단계·파일 조건·재현 정보를 기록할 수 있는 UTF-8 진단표를 제공한다. 상세 가이드 15개와 가이드 목록, 다국어 90개 URL을 합쳐 sitemap은 **106개 canonical URL**이다.
