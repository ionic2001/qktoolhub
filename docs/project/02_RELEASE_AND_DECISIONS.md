# 배포 이력과 결정 메모

문서 갱신일: 2026-09-16. 현재 코드 기준: `f417011`(원격 반영 후 운영 기준). 아래 앞부분은 9월 10일의 역사적 배포 기록이며, 현재 상태는 마지막 절을 기준으로 본다.

## 배포 기록

| 커밋 | 반영 내용 |
|---|---|
| 5b6e37d | 55개 정적 SEO 페이지, www canonical, 메타 통합, 내부 링크, 구조화 데이터, 공유 이미지, 실제 404 |
| c3dd57b | 환율 FAQ·수수료 예시 보강, 개인정보 범주 구분, 사용 가능 8개/예정 OCR 분리, 소개·문의 페이지 추가(60개 정적 SEO 페이지) |
| 9e1cc4d | 홈에서 출시 예정 OCR 영역과 관련 번역 데이터 제거 |
| d334ffb | Vercel에서 루트 파일이 언어별 홈 응답을 가리는 문제 수정 |
| cbfd3a0 | 5개 언어 설명 보강, 홈 카드 통일, 글자수 번역 누락과 기능 설명 불일치 수정 |
| f303402 | 이미지→PDF와 기존 PDF 도구 6개 경로 출시, 5개 언어·90개 canonical URL 확대 |
| b80aaff | 메인 PDF 메뉴를 큰 카드 2개·작업 바로가기 4개로 정리; 옛 `/pdf-converter` 홈 카드 제거 |
| 5996030 | 병합이 아닌 PDF 작업 화면의 파일 선택 문구를 단수에 맞게 수정 |
| aebdcad | 홈 하단 PDF 설명, 현재 도구 소개·문의·약관·방침 및 다국어 메타·관련 링크 갱신 |
| 9915d4f | 한국어 원본 PDF 가이드 3개, 검증 표본 5개, Article SEO 및 94개 canonical URL 검사 추가 |
| e7b85ba | PDF 개인정보·DPI 실험·도구 선택 가이드 추가, 도구별 내부 링크와 97개 URL 검사 |
| f27c0bd | 글자수·데이터 단위·이미지 비율 원본 가이드와 계산 회귀검사 추가, 100개 URL 검사 |
| adfe1f6 | 환율·서머타임·영업일 가이드, 환율 출처 표시와 모의 추세 제거, 103개 URL 검사 |
| 48cb3d3 | 온라인 서류 제출 7단계 핵심 가이드·다운로드 체크리스트·교차 도구 내부 링크, 104개 URL 검사 |
| 4483051 | 스캔 문서 촬영·PDF 변환·가독성 검수 가이드와 품질 체크리스트, 105개 URL 검사 |
| 21f4b77 | PDF 제출 오류 증상별 해결 가이드·진단표와 PDF 도구 내부 링크, 106개 URL 검사 |
| bd01288 | JPG 사진 여러 장을 PDF 한 파일로 만드는 실전 가이드·체크리스트, 5개 언어 이미지→PDF 설명 개선, 107개 URL 검사 |
| b6a2b01 | 모든 경로의 `?lang=ko` 별칭을 쿼리 없는 한국어 canonical 주소로 308 이동하고 회귀검사 추가 |
| a882301 | Vercel이 거절한 쿼리 제거 리디렉션 설정을 제거해 기존 canonical 방식과 정상 배포 구성 복원 |
| 484cf24 | 영어 Korean word counter 검색 의도에 맞춘 title·H1·description·초기 설명과 SEO 회귀검사 추가 |
| 0f5db64 | 한국어 환율 계산기 검색 의도·22개 지원 통화·ZWL 미지원 범위와 데이터 상태 문구 개선 |
| c6fd8db | 한국어 이미지 도트 변환 검색 의도와 실제 픽셀·색상·입력·출력 범위 설명 개선 |
| f417011 | 홈페이지→가이드 허브→16개 상세 가이드의 최초 HTML 링크 회귀검사 추가 |
| 92643cd | 환율 검색을 80개 통화·다국어 국가명으로 확장, 80개 대체 환율·API 응답 검증·출처 표기·다국어 SEO 설명 보완 |

## 결정 사항

- 3D 디오라마 스티커북은 QK Tool Hub와 제품·배포 주기가 달라 `ionic2001/diorama-stickerbook` 독립 저장소로 분리했다. QK Tool Hub에는 이미지 편집기 내부의 기존 스티커 기능만 유지한다.

- 단순 메타태그 증설보다 초기 HTML 내용·페이지별 검색 의도·내부 연결을 우선했다.
- 홈·도구·법적 안내 페이지를 언어별 URL로 구분했다.
- src/seo/catalog.ts를 공통 메타 데이터 진입점으로 사용한다. 기존 페이지별 SEO 훅의 중복 갱신은 제거했다.
- 새 SEO 빌드는 scripts/build-seo.mjs이다. 저장소에 남은 과거 build-*-seo 스크립트는 현재 package.json의 build에서 사용하지 않는다.
- 환율 설명에서 실시간 거래 데이터로 오해할 표현을 제거했다. 환율 계산의 전체 데이터 신뢰성 감사까지 완료한 것은 아니다.
- 환율 검색 범위를 22개에서 80개 통화로 늘렸다. 국제화 표준 지역명을 사용해 5개 언어의 국가명·통화명·코드를 모두 검색하고, 여러 국가의 공통 통화는 복수 지역을 연결한다. 무료 ExchangeRate-API의 하루 1회 갱신·출처 표기 조건을 UI에 반영했다.
- 파일의 브라우저 처리와 광고·분석 서비스 사용을 구분해 설명한다.
- Google 제목·설명 글자수는 고정 제한으로 표현하지 않는다.
- SEO 문서상의 초기 문제 목록은 과거 감사 기록이다. 현재 상태는 이 배포 메모와 기준 커밋을 우선한다.

## 검증 기록과 한계

2026-09-10 배포에서 빌드와 정적 SEO 검사 통과. 당시 운영 URL은 60개였고, 대표 55개 URL은 HTTP 200, 자기참조 canonical, 단일 H1, 언어 연결과 JSON-LD 검사를 통과했다. robots와 공유 이미지 정상, 없는 페이지·파일은 404 및 noindex를 확인했다.

4개 외국어 홈과 글자수 화면에서 메타·번역·설명 보완을 브라우저로 확인했다. 앞선 배포에서 글자수 샘플 계산 및 기존 단위 변환 테스트도 통과했다.

Google Search Console의 실제 색인 완료·검색 순위·Core Web Vitals·전체 도구 회귀는 별도 검증 대상이다. 사용자가 사이트맵 재제출 완료를 알렸다. 계정 데이터는 이 작업에서 직접 열람하지 않았다.

## 배포 참고

GitHub main 반영이 기존 Vercel 운영 배포를 유발한다. 앞으로 기능 브랜치와 미리보기에서 확인한 뒤 main에 통합하는 운영을 권장한다. 브랜치 보호나 필수 검사는 아직 설정했다고 간주하지 않는다.

2026-09-10 문서 업로드는 연결된 GitHub 도구로 배포했으며 당시 로컬 Git SHA가 원격과 다를 수 있었다. 현재 운영 기준은 원격 `main`의 `aebdcad`다. 새 컴퓨터는 원격 저장소의 최신 기록을 가져오고, 기존 체크아웃은 로컬 변경을 보존한 뒤 기록 차이를 검토해 동기화한다. 강제 덮어쓰기로 정리하지 않는다.

## 2026-09-16 현재 배포 결정과 검증

- 이미지→PDF(`/image-to-pdf`)와 기존 PDF 관리(`/pdf-tools`, `/pdf-merge`, `/pdf-split`, `/pdf-organize`, `/pdf-to-png`)를 분리해 출시했다. 선택한 페이지는 새 PDF **한 파일**로 추출하고 PNG는 선택 페이지를 저장한다. PDF 원문 수정·OCR·암호 입력·ZIP은 제공하지 않는다.
- 홈 PDF 영역은 주요 카드 2개와 병합·추출·정리·PNG 바로가기 4개다. 기존 `/pdf-converter`는 홈에서 제거했지만 실제 기능, 자기참조 canonical과 sitemap URL을 유지한다. 이전 여부는 Search Console 데이터로 결정한다.
- 5개 언어의 홈 하단 PDF 설명, 서비스 소개, 문의, 약관, 개인정보처리방침, 메타 설명과 정책 페이지 관련 도구 링크를 현재 기능에 맞춰 업데이트했다. 약관·방침의 개정 표기는 2026-09-16이다. 문서 첨부 없는 문의와 사용자가 메일에 첨부할 때의 이메일 처리 차이를 설명한다.
- 운영 코드 `aebdcad`에서 빌드와 90개 정적 URL SEO 검사, PDF 병합·추출·정리 및 테마 검사가 통과했다. Vercel 배포 성공과 공개 홈 5개 언어·소개·약관·방침 대표 언어의 새 문구·canonical, sitemap 90개를 확인했다. 전체 모바일·복잡한 PDF 표본·Search Console 계정 지표는 별도 검증 대상이다.
- 후속 코드 `9915d4f`는 Low value content 대응을 위해 `/guides`와 PDF 원본 가이드 3개를 추가했다. 가이드용 PDF 표본을 자동 검사하고, 한국어 전용 canonical·Article/CollectionPage JSON-LD·내부 링크·sitemap을 포함한 총 94개 URL 검사를 통과했다. 준비되지 않은 4개 언어 가이드 URL은 생성하지 않는다.

- 코드 `e7b85ba`는 브라우저 파일 처리 경계, A4·A3 DPI 출력 검증, PDF 작업 선택 가이드를 추가했다. 운영 렌더 계산을 자동 검사하고 한국어 PDF 도구의 초기 HTML에서 관련 글로 연결한다. 총 97개 URL 검사를 통과했다.
- 코드 `f27c0bd`는 PDF 밖의 콘텐츠로 한글·영문·이모지 바이트 비교, MB·MiB 차이, 이미지 비율 잠금 계산 가이드를 추가했다. 각 글은 운영 도구 함수의 고정 입력·결과를 자동 검사하며 글자수·단위 변환·이미지 편집 페이지의 초기 HTML에서 직접 연결한다. 가이드 목록과 상세 9개를 포함한 총 100개 URL 검사를 통과했다.
- 코드 `adfe1f6`는 환율 교차계산, 뉴욕 서머타임 경계, 한국 영업일 가이드를 추가했다. 외부 환율 실패 시 고정 참고값임을 5개 언어로 표시하고, 실제 과거 데이터 대신 생성하던 모의 추세를 제거했다. 총 103개 URL과 가이드 근거 검사를 통과했다.
- 2026-09-16 Search Console에서 글자수 바이트, MB·MiB, 이미지 비율 가이드는 아직 Google에 알려지지 않은 상태였고 세 URL 모두 우선 크롤링 대기열 등록을 완료했다.

- 코드 `48cb3d3`은 글자수 확인부터 사진 규격, 이미지→PDF, 병합·페이지 정리와 최종 검수까지 실제 제출 과정을 연결한 핵심 가이드를 추가했다. 여섯 관련 도구 페이지에서 가이드로 연결하고, UTF-8 체크리스트를 다운로드 자료로 제공한다. 총 104개 URL의 SEO 검사와 기존 기능 회귀 검사를 통과했다.

- 코드 `4483051`은 종이 문서 촬영·스캔 품질부터 이미지 방향·여백, 페이지 순서, 이미지→PDF, 결과 가독성 검수와 PDF 페이지 수정까지 연결한 실전 가이드를 추가했다. 네 관련 도구 페이지에서 연결하며 품질 체크리스트를 제공한다. 총 105개 URL의 SEO 검사와 기존 기능 회귀 검사를 통과했다.

- 2026-09-16 Search Console에서 서류 제출·스캔 PDF 가이드가 모두 `URL is unknown to Google`로 표시됐다. 두 URL의 색인 요청을 제출해 각각 우선 크롤링 대기열 등록 완료 메시지를 확인했다. `https://www.qktoolhub.com/sitemap.xml`도 다시 제출해 성공 메시지를 확인했으며, 발견 URL 수는 Google의 재처리 전 값인 `www` 60개·비-www 90개로 남아 있었다.

- 두 핵심 가이드를 운영 사이트 390px 뷰포트에서 검수했다. 문서 폭과 화면 폭은 390px로 일치했고 제목은 32px, 다운로드 카드는 358px 안에 표시됐다. 560px 최소 폭의 표는 페이지 전체를 밀어내지 않고 364px 표 컨테이너 내부에서만 가로 스크롤됐으며 관련 도구 링크는 줄바꿈됐다. 이 결과는 두 가이드와 해당 뷰포트에 한정한다.

- 코드 `21f4b77`은 파일 선택·용량·페이지·방향·가독성·서명/양식·브라우저 문제를 실패 단계별로 진단하는 PDF 제출 오류 가이드를 추가했다. PDF 압축·암호 해제·OCR·디지털 서명을 제공 기능으로 오해하지 않도록 범위를 명시하고, 여섯 PDF 도구 페이지에서 연결한다. 총 106개 URL의 SEO 검사와 기존 기능 회귀 검사를 통과했다.

- 2026-09-16 Search Console에서 PDF 제출 오류 해결 가이드는 `URL is unknown to Google`로 표시됐다. 색인 요청을 제출하고 `Indexing requested` 및 우선 크롤링 대기열 등록 메시지를 확인했다.

- 코드 `bd01288`은 Search Console에서 확인된 `jpg pdf 변환` 의도를 실제 `/image-to-pdf` 기능과 연결했다. 사진 순서, A4·원본 크기, 방향·여백, 2,400px 축소와 JPEG 정규화, 결과 페이지·가독성 검수를 다루는 한국어 원본 가이드와 체크리스트를 추가했다. 이미지→PDF의 5개 언어 이름·설명도 여러 JPG를 PDF 한 파일로 만드는 기능이 분명하도록 수정했으며, 총 107개 URL의 SEO·가이드·PDF·단위·테마 검사를 통과했다. 선택된 3개월 보고서는 2클릭·29노출 규모이므로 장기 검색 성과로 해석하지 않는다.

- 2026-09-16 Search Console에서 새 JPG PDF 변환 가이드는 `URL is unknown to Google`로 표시됐다. 색인 요청 뒤 `Indexing requested`와 우선 크롤링 대기열 등록 메시지를 확인했다. 같은 시점에 제출된 사이트맵은 모두 성공 상태였고 발견 URL 수는 `www` 105개·비-www 90개였다. 운영 sitemap의 107개와 차이가 있으므로 Google의 후속 처리를 계속 확인한다.

- 선택된 3개월 성과 보고서에는 비-www·HTTP·`?lang=ko` 주소가 별도 페이지 행으로 남아 있었다. 운영 응답을 직접 확인한 결과 비-www와 HTTP는 이미 www·HTTPS로 308 이동했다. `?lang=ko`의 쿼리 제거 리디렉션은 Vercel 설정 검증에서 거절돼 운영에 반영되지 않았다. 코드 `a882301`에서 해당 설정을 제거해 정상 배포 구성을 복원했으며, `?lang=ko`는 쿼리 없는 한국어 주소를 canonical로 지정하는 기존 방식으로 유지한다.

- 같은 보고서에서 `korean word counter` 5회와 `word counter korean` 3회 노출은 클릭으로 이어지지 않았다. 코드 `484cf24`는 영어 글자수 페이지의 제목·H1·설명과 초기 HTML을 Korean word·character·byte 계산 의도에 맞추고, 공백 기준 단어 계산과 UTF-8·EUC-KR 추정 차이를 실제 기능 범위 안에서 설명한다.

- 한국어 `/currency-converter`는 10회 노출·0클릭이었다. 코드 `0f5db64`는 제목·H1·설명을 달러·엔화·유로·원화 환산 의도에 맞추고 22개 지원 통화와 큰 금액 환산의 반올림·수수료 차이를 설명한다. 검색어에 나타난 짐바브웨 달러는 현재 지원하지 않으므로 ZWL·과거 발행 지폐의 값을 만들지 않고 미지원 범위를 명시한다. UI의 고정된 “실시간” 표현도 제공 환율과 참고값 상태에 맞게 수정했다.


- `이미지 도트 변환`은 1회 노출·0클릭이었다. 코드 `c6fd8db`는 `/pixel-art`의 한국어 제목·H1·설명을 해당 의도에 맞추고 JPG·PNG·WebP, 2~64px, 색상 팔레트, 컬러·흑백, 최대 20MB·4천만 픽셀, 1,600px 축소와 PNG 저장을 명시한다. 디더링과 손으로 그린 스프라이트 생성은 실제 기능이 아니므로 지원 범위에서 제외했다.

- 2026-09-16 Search Console에서 `/pixel-art`는 이미 Google에 색인된 상태였고 HTTPS 및 Breadcrumbs 1개가 정상으로 표시됐다. 배포 후 실시간 URL 검사도 `URL is available to Google`과 `Page can be indexed`를 확인했으며, 재색인을 요청해 우선 크롤링 대기열 등록 완료 메시지를 확인했다. 사이트맵은 성공 상태이고 발견 URL 수는 `www` 105개·비-www 90개로 유지됐다.

- 같은 날 `/currency-converter`도 Google 색인 완료, HTTPS 정상, Breadcrumbs 1개 유효 상태였다. 새 제목·지원 범위 설명 배포 후 실시간 검사에서 Google 접근과 색인 가능 상태를 확인하고 재색인을 요청해 우선 크롤링 대기열 등록을 완료했다.

- 영어 `/word-counter?lang=en`은 sitemap에서 발견됐지만 `Discovered - currently not indexed` 상태로 아직 크롤링 기록이 없었다. 실시간 검사에서는 Google 접근·색인 가능과 Breadcrumbs 1개 유효 상태를 확인했으며, 우선 크롤링 대기열에 색인 요청을 등록했다. 기술적 차단이 아니라 크롤링 대기 상태이므로 반복 요청 없이 후속 상태를 확인한다.

- 한국어 기본 `/word-counter`도 sitemap에서 발견됐으나 `Discovered - currently not indexed`로 아직 크롤링되지 않았다. 실시간 검사에서 Google 접근·색인 가능과 Breadcrumbs 1개 유효 상태를 확인하고 우선 크롤링 대기열 등록을 완료했다. 영어·한국어 URL 모두 기술적 차단보다 크롤링 대기 문제로 판단하며, 반복 요청 대신 상태 변화를 관찰한다.

- `/image-to-pdf`는 sitemap과 `/guides/image-formats-to-pdf` 내부 링크를 통해 발견됐지만 `Discovered - currently not indexed` 상태로 아직 크롤링 기록이 없었다. 실시간 검사에서 Google 접근·색인 가능과 Breadcrumbs 1개 유효 상태를 확인하고 우선 크롤링 대기열에 등록했다. `jpg pdf 변환` 검색 의도와 직접 연결되는 핵심 도구이므로 이후 색인 여부를 우선 확인한다.

- `/pdf-converter`는 Google 색인 완료 상태이며 사용자 선언 canonical과 Google 선택 canonical이 모두 자기 URL로 일치했다. 2026-09-10 모바일 Googlebot이 홈 내부 링크를 통해 정상 수집했고 크롤링·페이지 가져오기·색인이 모두 허용됐다. 2026-09-16 실시간 검사도 접근·색인 가능과 Breadcrumbs 유효 상태를 확인했다. 반면 `/image-to-pdf`는 아직 발견 후 크롤링 대기 상태이므로 기존 URL의 검색 신호를 보존하기 위해 리디렉션하지 않고 유지한다.

- `/pdf-merge`는 sitemap과 `/image-to-pdf` 내부 링크에서 발견됐지만 `Discovered - currently not indexed` 상태로 아직 크롤링 기록이 없었다. 실시간 검사에서 Google 접근·색인 가능과 Breadcrumbs 1개 유효 상태를 확인하고 우선 크롤링 대기열 등록을 완료했다.

- `/pdf-split`은 sitemap과 `/guides/pdf-page-extraction-test` 내부 링크에서 발견됐지만 `Discovered - currently not indexed` 상태로 아직 크롤링 기록이 없었다. 실시간 검사에서 Google 접근·색인 가능과 Breadcrumbs 1개 유효 상태를 확인하고 우선 크롤링 대기열 등록을 완료했다.

- `/pdf-organize`는 sitemap과 `/guides/pdf-page-extraction-test` 내부 링크에서 발견됐지만 `Discovered - currently not indexed` 상태로 아직 크롤링 기록이 없었다. 실시간 검사에서 Google 접근·색인 가능과 Breadcrumbs 1개 유효 상태를 확인하고 우선 크롤링 대기열 등록을 완료했다.

- `/pdf-to-png`은 sitemap에서 발견됐지만 `Discovered - currently not indexed` 상태로 아직 크롤링 기록이 없었고, Search Console의 참조 페이지 정보는 가져오지 못한 것으로 표시됐다. 실시간 검사에서는 Google 접근·색인 가능과 Breadcrumbs 1개 유효 상태를 확인했으므로 실제 접근 장애로 보지 않으며, 우선 크롤링 대기열 등록을 완료했다.

- PDF 도구 허브 `/pdf-tools`는 `URL is unknown to Google`로 표시됐고 sitemap·참조 페이지도 아직 보고되지 않았다. 운영 sitemap 107개와 Search Console의 `www` 발견 105개 차이에서 처리되지 않은 URL일 수 있다. 실시간 검사에서는 Google 접근·색인 가능과 Breadcrumbs 1개 유효 상태를 확인했고 우선 크롤링 대기열 등록을 완료했다.

- 콘텐츠 허브 `/guides`는 sitemap에서 발견됐지만 `Discovered - currently not indexed` 상태로 아직 크롤링 기록이 없고 참조 페이지도 보고되지 않았다. 실시간 검사에서는 Google 접근·색인 가능과 Breadcrumbs 1개 유효 상태를 확인해 우선 크롤링 대기열에 등록했다. Low value content 재검토 전에 허브와 핵심 가이드의 실제 색인을 우선 추적한다.

- 이전에 색인을 요청한 `/guides/jpg-images-to-one-pdf`를 다시 확인했으나 여전히 `URL is unknown to Google`이고 sitemap·참조 페이지도 보고되지 않았다. 2026-09-16 후속 실시간 검사에서는 Google 접근·색인 가능과 Breadcrumbs 1개 유효 상태를 재확인했다. 동일 URL을 반복 제출해도 우선순위가 바뀌지 않으므로 추가 요청은 하지 않고 발견·크롤링 상태 변화를 기다린다.

- 최초 HTML 내부 링크 감사에서 5개 언어 홈페이지의 `/guides` 링크, 가이드 허브의 16개 상세 가이드 링크, JPG→PDF 가이드로 향하는 이미지→PDF·기존 PDF 변환·이미지 편집 링크가 모두 일반 `<a href>`로 존재함을 확인했다. 코드 `f417011`은 홈페이지→허브와 허브→전체 상세 가이드 연결을 정적 SEO 검사에 추가했다. Search Console의 참조 페이지 미보고만으로 링크 누락으로 판단하지 않는다.
